# -Lead_Collection_Automation
A real-time, event-driven data pipeline built on n8n that automates lead ingestion. It exposes a secure webhook endpoint to capture incoming user data (Name, Email, Phone) from external forms or landing pages and instantaneously syncs it with the Google Sheets API for structured tracking
